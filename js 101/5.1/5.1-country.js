

let language, isIsland, population, country;

const countryToLiveIn = function(language, isIsland, population, country) {
  if(language = 'English' && (isIsland = false) && population < 50000000 && (country = 'Denmark') ) {
      return "Denmark is the country for you"
  }

  else {"Denmark is not the country for you"}
}

